---
description: The one-session development loop — orient → domain skill → tdd → deslopify → clean commits → self-review → ship.
argument-hint: "<the change>"
---

Use the `workflows/ship-change` skill for: $ARGUMENTS

If it turns out to be bigger than one session, stop and switch to `workflows/new-feature`.
