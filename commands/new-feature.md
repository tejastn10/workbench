---
description: Orchestrate a multi-session feature end to end — grill → PRD → design → phased delivery → per-phase build/review/ship.
argument-hint: "<what the feature is>"
---

Use the `workflows/new-feature` skill to build: $ARGUMENTS

Follow its stage table. Confirm the phase list with me before starting any code.
