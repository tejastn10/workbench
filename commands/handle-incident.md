---
description: Run a production incident — on-call → response → mitigate → root cause → hotfix → postmortem.
argument-hint: "<what's broken>"
---

Use the `workflows/handle-incident` skill. Situation: $ARGUMENTS

Restore service first. Do not investigate root cause while the site is down.
